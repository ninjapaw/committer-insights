{
  "name": "@ninjapaw/committer-insights",
  "version": "0.1.0",
  "private": true,
  "description": "Local Azure DevOps and GitHub committer reports with CSV, PDF, and HTML exports.",
  "license": "MIT",
  "author": "Microsoft Corporation",
  "repository": {
    "type": "git",
    "url": "https://github.com/ninjapaw/committer-insights.git"
  },
  "type": "module",
  "engines": {
    "node": "24.19.0",
    "npm": "11.17.0"
  },
  "packageManager": "npm@11.17.0",
  "workspaces": [
    "app",
    "api",
    "packages/contracts",
    "demo"
  ],
  "scripts": {
    "dev": "npm run build && npm run start --workspace api",
    "prebuild": "node scripts/clean-build.mjs",
    "build": "npm run build --workspace packages/contracts && npm run build --workspace app && npm run build --workspace api",
    "build:exe": "npm run build && node scripts/build-executable.mjs",
    "test:exe": "node scripts/test-executable-bundle.mjs && node scripts/smoke-executable.mjs",
    "demo:generate": "npm run build --workspace packages/contracts && npm run build --workspace api && node scripts/generate-demo.mjs",
    "demo:build": "npm run demo:generate && npm run build --workspace demo",
    "demo:test": "npm run demo:build && node scripts/test-demo.mjs",
    "demo:dev": "npm run dev --workspace demo",
    "lint": "eslint . --max-warnings 0",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "npm run build --workspace packages/contracts && npm run typecheck --workspace app && npm run typecheck --workspace api",
    "test": "npm run test:unit && npm run test:integration",
    "test:unit": "npm run test:unit --workspace app && npm run test:unit --workspace api",
    "test:integration": "npm run test:integration --workspace api",
    "ci": "npm run format:check && npm run lint && npm run typecheck && npm run test && npm run build:exe && npm run test:exe"
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "@playwright/test": "1.63.0",
    "@typescript-eslint/eslint-plugin": "^8.15.0",
    "@typescript-eslint/parser": "^8.15.0",
    "esbuild": "^0.28.2",
    "eslint": "^9.15.0",
    "eslint-config-prettier": "^9.1.0",
    "postject": "^1.0.0-alpha.6",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.15.0",
    "vitest": "^5.0.1"
  }
}
