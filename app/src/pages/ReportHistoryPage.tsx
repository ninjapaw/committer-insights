export function ReportHistoryPage(): JSX.Element {
  return (
    <section aria-labelledby="history-title">
      <h1 id="history-title">Report history</h1>
      <p>Shown only when retention is enabled for a saved report.</p>
    </section>
  );
}
