export function SecurityPrivacyPage(): JSX.Element {
  return (
    <section aria-labelledby="privacy-title">
      <h1 id="privacy-title">Security and privacy</h1>
      <p>
        Azure tokens and generated reports remain inside this local application. Data is written to
        disk only when you download an export.
      </p>
    </section>
  );
}
