export function IdentityReviewPage(): JSX.Element {
  return (
    <section aria-labelledby="identity-review-title">
      <h1 id="identity-review-title">Identity review</h1>
      <p>Identities that could not be automatically matched are shown here for manual review.</p>
    </section>
  );
}
